# Ingestion Collector Submodule

Dieses Verzeichnis ist als Submodule `whatsthat` vorgesehen.
Führe nach dem Klonen:

```bash
git submodule update --init --recursive
```

um den Collector bereitzustellen.
