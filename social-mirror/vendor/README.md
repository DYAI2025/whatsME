# Vendor Dependencies

Die hier referenzierten Submodule enthalten optionale Marker-Bestände und Frameworks.
Initialisiere sie bei Bedarf mit:

```bash
git submodule update --init vendor/WTME vendor/LeanDeep
```
